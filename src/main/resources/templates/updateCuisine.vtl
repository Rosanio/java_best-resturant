<h2>Update $cuisine.getType()</h2>

<form class="form-group" action="/$user.getId()/cuisines/$cuisine.getId()/updated" method="post">
  <label for="update">Update Cuisine Name</label>
  <input type="text" name="update" required>
  <button type="submit" class="btn btn-default">Update</button>
</form>


<a href="/$user.getId()/cuisines">Best Restaurants Home</a>
